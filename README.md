# webclient
webclient

# Build
```bash
    $ npm install -g gulp tsd bower
    $ npm install --production
    $ tsd install
    $ cp config.example.json config.json # edit as needed
    $ gulp build
```
